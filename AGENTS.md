we use git vcs and jujutsu vcs

always try jujutsu vcs first if its not available then use git

in any project. do not ever run cargo fmt or any formatting.

never run any formatting please.

to run any build command use "nix develop ../nix-devshells#chart --command {command}"
